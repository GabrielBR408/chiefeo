# Frontend env. Copy to .env.local for local dev; set the same keys in
# Vercel → Project Settings → Environment Variables for production.
# Both values are safe to expose to the browser — RLS protects user data.
VITE_SUPABASE_URL=https://xxxx.supabase.co
VITE_SUPABASE_ANON_KEY=eyJ...
